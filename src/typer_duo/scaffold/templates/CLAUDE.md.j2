# {{ project_name }}

## CLI Commands

```
{{ project_name }} hello [--json] [--name TEXT]
```

All commands support `--json` for agent orchestration (JSON to stdout, human text to stderr).

## Running Tests

```bash
.venv/bin/pytest
```
